# 阿里云日志服务（SLS）环境变量配置
# 请将此文件复制为 .env 并填入你的实际凭证

# 阿里云访问凭证（必需）
SLS_ACCESS_KEY_ID=你的AccessKey_ID
SLS_ACCESS_KEY_SECRET=你的AccessKey_Secret

# 可选配置
# SLS_DEFAULT_REGION=cn-hangzhou
# SLS_DEFAULT_PROJECT=my-python-logs

# 获取阿里云凭证的步骤：
# 1. 登录阿里云控制台: https://ecs.console.aliyun.com/
# 2. 点击右上角头像 → AccessKey 管理
# 3. 创建新的 AccessKey 或使用现有的
# 4. 将 AccessKey ID 和 AccessKey Secret 填入上面的配置

# 安全提醒：
# - 不要将包含真实凭证的 .env 文件提交到版本控制系统
# - .env 文件已添加到 .gitignore 中
# - 定期轮换你的 AccessKey